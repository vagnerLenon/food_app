import { CiForkAndKnife } from "react-icons/ci";

import { IIconProps } from "@/Utils/Interfaces";

export default function GarfoEFaca({ size, color, className }: IIconProps) {
  return <CiForkAndKnife size={size} color={color} className={className} />;
}
